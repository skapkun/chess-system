package boardgame;

public class Piece {
	protected Position position; 
	private Board board;
	public Piece(Board board) { 
		this.board = board;
		position = null; //mesmo não atribuindo, por padrão java atribui valor nulo
	}
	public Board getBoard() {
		return board;
	} 

}
